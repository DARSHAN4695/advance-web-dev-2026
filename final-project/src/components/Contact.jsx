function Contact() {
  return (
    <section className="section contact-section" id="contact">
      <div>
        <p className="eyebrow">Contact</p>
        <h2>Ready for review, Docker setup, and a screenshot demo.</h2>
      </div>
      <div className="contact-panel">
        <p>
          The page is structured as reusable React components and can be run in
          Docker with the provided configuration files.
        </p>
        <a className="button button-primary" href="mailto:hello@asterstudio.example">
          hello@asterstudio.example
        </a>
      </div>
    </section>
  );
}

export default Contact;
